import Vue from "vue";
import VueScrollTo from "vue-scrollto";

Vue.use(VueScrollTo, {
  duration: 500, // Duración de la animación en milisegundos
  easing: "ease", // Tipo de curva de animación
});
